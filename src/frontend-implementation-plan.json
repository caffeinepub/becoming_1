{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Remove Total Volume row from Monthly Volume Summary",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Remove the “Total Volume” tile from the Monthly Volume Summary so only Total Reps and/or Total Time may render.",
      "acceptanceCriteria": [
        "In frontend/src/features/becoming/components/MonthlyVolumeSummary.tsx, the UI no longer renders the block with label “Total Volume”, regardless of whether numeric-volume habits exist.",
        "Monthly Volume Summary continues to correctly render “Total Reps” when reps-based habits exist and “Total Time” when time-based habits exist.",
        "No other part of the app (e.g., habit rows, edit modal, or any other summaries) is changed as part of this update."
      ],
      "file_operations": [
        {
          "path": "frontend/src/features/becoming/components/MonthlyVolumeSummary.tsx",
          "operation": "modify",
          "description": "Remove rendering of the “Total Volume” tile/row in Monthly Volume Summary only. Ensure the component never displays a Total Volume item (remove/ignore hasNumeric and totalNumeric UI block as needed) while keeping Total Reps and Total Time behavior unchanged. Keep this change isolated to this file to avoid impacting other summaries or habit UI."
        }
      ]
    }
  ]
}